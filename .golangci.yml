version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - gocritic
    - revive
  settings:
    revive:
      rules:
        - name: exported
          arguments: ["checkPublicInterface"]

formatters:
  enable:
    - gofmt
    - goimports
